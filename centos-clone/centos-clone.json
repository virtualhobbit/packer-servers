{
  "builders": [
    {
      "type": "vsphere-clone",

      "vcenter_server":      "{{user `vcenter_server`}}",
      "username":            "{{user `username`}}",
      "password":            "{{user `password`}}",
      "insecure_connection": "true",

      "template": "CentOS 7",
      "vm_name":  "CentOS 7 (DEV)",
      "cluster": "{{user `cluster`}}",

      "communicator": "none"
    }
  ]
}
